spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_ols?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: spring_ols
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
  config:
    import: classpath:jwt.yml

logging:
  file:
    name: /logs/application.log
  level:
    root: debug